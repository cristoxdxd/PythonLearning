FruitsList = ['Apple','Pear','Banana']

for fruit in FruitsList:
    print(fruit)

for item in range(0,5)
print('Elemento {}',.format(item))

matrix = [
    [0,1,0],
    [1,1,1],
    [0,0,2]
]

for row in matrix:
    for columns in matrix:
        print(row)
        print(columns)

# Anidados basados en índices
for i in range(len(matrix):
    for j in range(len(matrix[i])):  
        print('Value of position i:{} - j:{} is:{}'.format(i,j,matriz[i][j]))
        if matriz[i][j] == 2:
            matriz[i][j] = 10

print(matriz)